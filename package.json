{
    "name": "frequency-matrix",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "start": "parcel -p 9000 ./index.html --no-cache --out-dir dist-dev",
        "build": "parcel build ./index.html --public-url ./ --no-cache --no-source-maps --out-dir dist-prod",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "parcel-bundler": "^1.12.5"
    },
    "dependencies": {
        "xlsx-parse-json": "^3.1.1"
    },
    "browserslist": [
        "last 1 Chrome versions"
    ]
}